$('#follow').html('<%= button_to "follow del negozio", follow_store_path(@store), method: :post, remote: true, class: "waves-effect waves-light btn-large" %>'+"<h1> numero followerZ <%= @store.followers.count %> </h1>");
