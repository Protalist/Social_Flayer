<%= image_tag @photo.image.superpic.url, class: "materialboxed" %>

<%if can? :destroy_photo, @store%>
<%=link_to "destroy" , destroy_photo_store_path(params[:id],params[:picture_id]), method: :delete, class: "waves-effect waves-light btn" %>
<%end%>
